spring:
  application:
    name: infrastructure-test
  # Database configuration is provided by profile-specific files
  # (application-local-test.yml, application-dev-test.yml, application-prod-test.yml)
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  data:
    redis:
      host: localhost
      port: 6379
      password: 

jwt:
  secret: testsecretkeytestsecretkeytestsecretkey
  access-token-expiration: 3600
  refresh-token-expiration: 604800
